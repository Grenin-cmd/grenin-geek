services:
  - type: web
    name: grenin-geek-store
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SUPABASE_DB_URL
        sync: false
      - key: FRONTEND_ORIGIN
        sync: false
